// Generated by CoffeeScript 1.12.7
(function() {
  var array, arrayAnalyzer, elapsed_time, i, j, new_array, num, sum_1to1m, sum_1to500;

  arrayAnalyzer = (function(_this) {
    return function(arr) {
      var element, i, len, results;
      results = [];
      for (i = 0, len = arr.length; i < len; i++) {
        element = arr[i];
        results.push(console.log(element + ":" + (typeof element)));
      }
      return results;
    };
  })(this);

  array = [
    3, 5, 'Dojo', 'rocks', {
      name: 'Michael',
      title: 'Sensei'
    }
  ];

  new_array = ["This", "is", "a", "new", "array"];

  array.push(100);

  array.push(new_array);

  arrayAnalyzer(array);

  sum_1to500 = 0;

  for (num = i = 1; i <= 500; num = ++i) {
    sum_1to500 += num;
  }

  sum_1to1m = 0;

  elapsed_time = performance.now();

  for (num = j = 1; j <= 1000000; num = ++j) {
    sum_1to1m += num;
  }

  console.log("Sum of number from 1 to 500: " + sum_1to500);

  console.log("Sum of number from 1 to 1 Milion: " + sum_1to1m);

  console.log("Elapsed Time: " + (performance.now() - elapsed_time) + "ms");

}).call(this);
